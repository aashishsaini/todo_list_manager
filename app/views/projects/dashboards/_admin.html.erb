<table class="table table-stripped">
  <thead>
    <tr>
      <th>Status</th>
      <% @projects.map(&:users).flatten.uniq.each do |user| %>
        <th><%= user.name %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% admin_results.each do |status, todos| %>
      <tr>
        <td><%= status %></td>
        <% todos.each do |project_todo| %>
          <td><%= (project_todo.values.flatten.compact.map(&:name).join("<br>").html_safe) %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
